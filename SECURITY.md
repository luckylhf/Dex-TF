# 安全声明（Safety & Security Policy）

> **本项目是一个可以控制真实机器人的软件。误用可能造成设备损坏或人身伤害。**

## 1. 硬件安全（最重要）

`interactive_control.launch.py` 启动的 GUI **会向真实机器人下发位置控制指令**。
使用前请务必确认：

1. **机器人处于安全状态**
   - 机器人已**可靠吊装或固定**，或周围有足够的安全空间；
   - 机器人的急停按钮（E-Stop）处于**触手可及**的位置；
   - 确认执行范围内**没有人员**、障碍物或易损设备。

2. **指令参数合理**
   - `interactive_gui.py` 默认下发的速度上限为 `0.2`（`SetMotorPosition.spd`），
     电流上限为 `8.0 A`（`SetMotorPosition.cur`）。首次调试建议保持或进一步调低，
     **切勿盲目放大**；
   - 滑块目标位置与当前真实位姿差值过大时，机器人会以设定速度**长距离运动**，
     请先用 "Sync to Real" 同步，再小幅度拖动。

3. **连接状态**
   - GUI 仅在收到 `/robot_online_status = true` 时才允许点击 "Execute"，
     该状态基于 1 秒超时判定，**不能作为唯一的碰撞防护手段**；
   - 网络抖动、桥接节点重启都可能造成状态误判，实际操作应始终有人在场监护。

4. **仅在仿真中验证过的参数不要直接下发真机**，包括关节限位、零位偏置、
   方向符号等。本仓库的 `urdf/` 包含两套略有差异的关节限位
   （见 `docs/model.md`），使用前请与机器人实际固件参数核对。

## 2. 网络与信息安全

- 本项目的通信基于 **DDS（ROS 2 默认中间件）**，默认**不带加密与认证**。
  请仅在**可信的内网/专网**中使用；不要在公网暴露 DDS 端口或 ROS 2 话题。
- 若需要跨网段/公网通信，请自行叠加安全层（如 `rmw_zenoh` + TLS、
  VPN、`ros2 security` / SROS2 等）。
- 仓库中不包含任何凭据、密钥或内部服务地址。请勿在提交的代码中写入
  机器人 IP、账号口令、厂商内部仓库地址等信息。

## 3. 报告安全问题

如发现可能导致设备损坏、人身伤害或远程越权控制的问题，请**不要**直接
提交公开 Issue，而应通过 GitHub 的私密漏洞报告功能私下联系维护者：

1. 打开 <https://github.com/luckylhf/Dex-TF/security/advisories/new>
   （或仓库页面 → **Security** → **Report a vulnerability**）；
2. 在报告中说明：受影响的版本/commit、复现步骤、可能的后果；
3. 若你已有修复方案，可在同一私密线程中附上补丁。

我们会在收到报告后尽快确认并回复。在修复发布前，请不要公开披露细节。

## 4. 免责声明

本项目按 "AS IS" 提供，不附带任何明示或暗示的担保。因使用本项目
（包括但不限于向真实机器人下发指令）而导致的任何设备损坏、财产损失
或人身伤害，作者与贡献者不承担责任。使用风险由使用者自行承担。
